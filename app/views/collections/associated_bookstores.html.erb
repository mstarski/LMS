<h1>Bookstores that own collection <%= link_to "#{@collection.name}", collection_path(@collection) %></h1>
<ul>
  <% @bookstores.each do |bookstore| %>
    <li>
      <%= bookstore.name %> (id: <%= bookstore.id%>) <%= link_to 'Show', bookstore_path(id: bookstore.id) %>
    </li>
  <% end %>
</ul>
